spring:
  application:
    name: smarttimesheet-main-service
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 0
  
management:
  endpoints:
    web:
      exposure:
        include: "*"
